#ifndef SERVER_H
#define SERVER_H

#include <stdio.h>
#include <stdlib.h>
#include <errno.h>
#include <string.h>

#include "../network/network_server.h"
#include "module.h"
#include "../curl/curl.h"

void app(const int port,char* ipserver);
void clear_modules(List * modules);
void remove_module(List * modules, Node * module);
#endif // SERVER_H

